[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "net-monitor"
version = "0.1.0"
description = "Windows 11 desktop network monitor"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "PySide6",
  "psutil",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
